import React from 'react';
import '../Styles/DHome.css'; // ✅ Import the CSS

function DHome() {
    return (
        <div className="donor-home">
            <h1>Welcome, Donor (Individual)!</h1>
            <p>You’re doing a life-saving job. Thank you! ❤️</p>
        </div>
    );
}

export default DHome;
